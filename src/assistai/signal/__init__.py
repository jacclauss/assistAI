"""Signal channel: receive DMs, enforce the allowlist, send replies.

Submodules are imported directly (``assistai.signal.client``, and so on) so
``assistai.config`` can use ``assistai.signal.numbers`` without a cycle.
"""
